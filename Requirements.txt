# Agent Framework & LLM Integration
openai>=1.10.0
langchain>=0.1.0
langchain-openai>=0.0.5
python-dotenv>=1.0.0

# RAG & Document Processing
docling>=1.0.0            # IBM's document parsing tool mentioned in report
chromadb>=0.4.20          # Vector store (likely used for RAG memory)
tiktoken>=0.5.0

# Machine Learning (Anomaly Detection)
torch>=2.1.0              # PyTorch for the LSTM model
numpy>=1.24.0
scikit-learn>=1.3.0       # For metrics and data splitting

# Data Handling (Time-Series)
pandas>=2.1.0
matplotlib>=3.8.0         # For visualizing sensor data and anomalies
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0